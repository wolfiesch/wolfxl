//! `xl/styles.xml` emitter — full styles file assembled from [`StylesBuilder`].
//! Wave 2A.

use crate::model::format::StylesBuilder;

pub fn emit(_styles: &StylesBuilder) -> Vec<u8> {
    Vec::new()
}
